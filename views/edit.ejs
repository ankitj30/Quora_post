<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>edit post </title>
        <link rel="stylesheet" href="/style.css" />
</head>
<body>
        <div class="container">
            <nav class="nav">
                <div class="brand">
                    <div class="logo">AJ</div>
                    <div class="site">Quora Mini</div>
                </div>
                <div class="nav-menu">
                    <a href="/posts/<%= post.id %>">Back</a>
                    <a href="/posts/new" class="btn">Create</a>
                </div>
            </nav>

            <main style="margin-top:14px">
                <h2>Edit Post</h2>
                <p style="color:var(--muted);font-size:13px">Post id: <%= post.id %></p>
                <p style="color:var(--muted);font-size:13px">Post username: <%= post.username %></p>

                <form method="post" action="/posts/<%= post.id %>?_method=PATCH">
                    <div class="form-row">
                        <label for="title">Post</label>
                        <textarea id="title" name="title"><%= post.title %></textarea>
                    </div>
                    <div class="center">
                        <button class="btn" type="submit">Save</button>
                    </div>
                </form>
            </main>
        </div>
</body>
</html>